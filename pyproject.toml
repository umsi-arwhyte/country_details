[project]
name = "country-details"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "countrydetails>=1.0.8",
]

[tool.ruff]
line-length = 100  # Default: 88
preview = true  # Enable unstable linting and formatting
